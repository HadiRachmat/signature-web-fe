const MemorizeRecordPage = () => {
  return (
    <div>
      <h1>Memorize Record Page</h1>
      <p>This is the memorize record page, accessible to Admin, Student, and Teacher roles.</p>
    </div>
  );
};

export default MemorizeRecordPage;
